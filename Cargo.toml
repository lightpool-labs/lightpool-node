[package]
name = "lightpool"
version = "0.4.0"
edition = "2021"
authors = ["LightPool Team"]
license = "Apache-2.0"
description = "LightPool node release package (extracts binaries and sets PATH)"

[lib]
path = "src/lib.rs"

[build-dependencies]
