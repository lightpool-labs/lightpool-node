# lightpool-node
High-Frequency Trading Infrastructure Powered by a Layer 1 Blockchain

#### Create Node
```shell
$ lightpool -- create --datadir ./data/node1
```
#### Run Node
```shell
$ lightpool -- -vv run --keys data/node1/node.json --committee data/node1/committee.json --store data/node1/store
```
